<Window x:Class="BDKR.AddStoregeRoomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BDKR"
        mc:Ignorable="d"
        Title="Добавление склада" Height="189" Width="517">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <TextBox Text="Выберите адресс"
                 VerticalAlignment="Center"
                 HorizontalAlignment="Center"
                 FontSize="20"/>

        <ComboBox Grid.Column="1"
                  x:Name="AdressCombo"
                  VerticalAlignment="Center"
                  FontSize="20"
                  DisplayMemberPath="Adress"/>

        <Button Grid.Row="1"
                Grid.ColumnSpan="2"
                Content="Добавить склад"
                FontSize="20"
                VerticalAlignment="Center"
                HorizontalAlignment="Center"
                x:Name="AddStoreg"
                Click="AddStoreg_Click"/>
    </Grid>
</Window>
